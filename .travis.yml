language: cpp
compiler:
  - clang
  - gcc
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y cmake
  - sudo apt-get install -y libboost-all-dev
install: true
before_script:
  - mkdir -p build
  - cd build
script:
  - cmake ..
